name: Build TFLite Support Android AAR (Bazel 7.0.0 with Platforms)

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  build-and-package:
    name: "Build TFLite Support AAR (all arch)"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      # 强制删除 .bazelversion 文件，防止干扰
      - name: Remove .bazelversion
        run: |
          if [ -f .bazelversion ]; then
            rm -v .bazelversion
            echo "Removed .bazelversion"
          else
            echo "No .bazelversion found"
          fi

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # 手动下载并安装 Bazel 7.0.0
      - name: Install Bazel 7.0.0
        run: |
          BAZEL_VERSION=7.0.0
          wget -q https://github.com/bazelbuild/bazel/releases/download/${BAZEL_VERSION}/bazel-${BAZEL_VERSION}-linux-x86_64 -O bazel
          chmod +x bazel
          sudo mv bazel /usr/local/bin/bazel
          echo "Bazel installed:"
          bazel --version

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Export Android environment
        run: |
          echo "ANDROID_HOME=$ANDROID_HOME" >> "$GITHUB_ENV"
          echo "ANDROID_SDK_HOME=$ANDROID_HOME" >> "$GITHUB_ENV"

      - name: Cache Bazel external dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/bazel
          key: ${{ runner.os }}-bazel-${{ hashFiles('WORKSPACE', '**/WORKSPACE', '**/*.bzl') }}
          restore-keys: |
            ${{ runner.os }}-bazel-

      # 修补 BUILD 文件中的错误 load 路径
      - name: Patch BUILD file to use correct rules_java load path
        run: |
          sed -i 's|//third_party/bazel_rules/rules_java/java:defs.bzl|@rules_java//java:defs.bzl|' tensorflow_lite_support/java/BUILD
          echo "Patched BUILD file"

      # 修补 third_party/repo.bzl 中的 to_list() 错误
      - name: Patch third_party/repo.bzl to fix to_list error
        run: |
          sed -i 's/\.to_list()//g' third_party/repo.bzl
          echo "Patched third_party/repo.bzl"

      # 修补 flatbuffers workspace.bzl 添加镜像 URL
      - name: Patch flatbuffers workspace.bzl to add mirror URL
        run: |
          FLATBUFFERS_WORKSPACE="third_party/flatbuffers/workspace.bzl"
          if [ -f "$FLATBUFFERS_WORKSPACE" ]; then
            # 在 urls = [ 行后插入镜像URL作为第一项（假设版本为 v2.0.0，可根据实际版本调整）
            sed -i '/urls = \[/a \        "https://mirror.tensorflow.org/github.com/google/flatbuffers/archive/v2.0.0.tar.gz",' "$FLATBUFFERS_WORKSPACE"
            echo "Patched $FLATBUFFERS_WORKSPACE"
          else
            echo "Warning: $FLATBUFFERS_WORKSPACE not found"
          fi

      # 创建 Android 平台定义（符合 Bazel 7.0+ 规范）
      - name: Create Android platform definitions
        run: |
          mkdir -p platform
          cat > platform/BUILD <<EOF
          platform(
              name = "android_arm64_v8a",
              constraint_values = [
                  "@platforms//os:android",
                  "@platforms//cpu:arm64",
              ],
          )

          platform(
              name = "android_armeabi_v7a",
              constraint_values = [
                  "@platforms//os:android",
                  "@platforms//cpu:armv7",
              ],
          )

          platform(
              name = "android_x86",
              constraint_values = [
                  "@platforms//os:android",
                  "@platforms//cpu:x86_32",
              ],
          )

          platform(
              name = "android_x86_64",
              constraint_values = [
                  "@platforms//os:android",
                  "@platforms//cpu:x86_64",
              ],
          )
          EOF
          echo "Created platform/BUILD with Android platform definitions."

      # 计算 Python 库路径（用于 Hermetic Python）
      - name: Compute Python library path
        id: python
        run: |
          echo "lib_path=$(python3 -c 'import site; print(site.getsitepackages()[0])')" >> "$GITHUB_OUTPUT"

      # 先拉取所有依赖，确保 Python 工具链正确初始化
      - name: Fetch external dependencies with Python env
        run: |
          bazel fetch //tensorflow_lite_support/java:tensorflow-lite-support \
            --repo_env=HERMETIC_PYTHON_VERSION=3.12 \
            --repo_env=PYTHON_BIN_PATH=/usr/bin/python3 \
            --repo_env=PYTHON_LIB_PATH=${{ steps.python.outputs.lib_path }} \
            --repo_env=TF_PYTHON_VERSION=3.12 \
            --curses=no \
            --noenable_bzlmod

      # 正式构建，使用新平台标志并禁用 Bzlmod
      - name: Build TFLite Support AAR
        run: |
          bazel build //tensorflow_lite_support/java:tensorflow-lite-support \
            --android_platforms=//platform:android_arm64_v8a,//platform:android_armeabi_v7a,//platform:android_x86,//platform:android_x86_64 \
            --repo_env=HERMETIC_PYTHON_VERSION=3.12 \
            --repo_env=PYTHON_BIN_PATH=/usr/bin/python3 \
            --repo_env=PYTHON_LIB_PATH=${{ steps.python.outputs.lib_path }} \
            --repo_env=TF_PYTHON_VERSION=3.12 \
            --curses=no \
            --show_timestamps \
            --verbose_failures \
            --noenable_bzlmod

      - name: Locate AAR file
        id: locate-aar
        run: |
          AAR_PATH=$(find bazel-bin/tensorflow_lite_support/java -name "*.aar" | head -n 1)
          if [ -z "$AAR_PATH" ]; then
            echo "Error: AAR not found!"
            exit 1
          fi
          echo "aar-path=$AAR_PATH" >> "$GITHUB_OUTPUT"
          echo "Found AAR at $AAR_PATH"

      - name: Upload AAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: tflite-support-android-aar-all-arch
          path: ${{ steps.locate-aar.outputs.aar-path }}
          if-no-files-found: error

      - name: List output files
        run: find bazel-bin/tensorflow_lite_support/java -type f || true
